# data-model-instrospection

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-model-instrospection` to build the library.

## Running unit tests

Run `nx test data-model-instrospection` to execute the unit tests via
[Jest](https://jestjs.io).
