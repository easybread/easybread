# schema-org

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build schema-org` to build the library.

## Running unit tests

Run `nx test schema-org` to execute the unit tests via
[Jest](https://jestjs.io).
