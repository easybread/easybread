# data-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-model` to build the library.

## Running unit tests

Run `nx test data-model` to execute the unit tests via
[Jest](https://jestjs.io).
